// TODO: import { useState } from 'react'
export default function App() {
  // TODO: Initialize state: const [count, setCount] = useState(0)

  return (
    <div>
      <button
        type="button"
        onClick={() => {
          // TODO: Replace with increment logic: setCount(count + 1)
        }}
      >
        {/* TODO: Display the count here */}
        Clicks:
      </button>
    </div>
  );
}
